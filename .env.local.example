# Copiar para .env.local e preencher com as credenciais do projeto Supabase
# (Dashboard → Project Settings → API).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Nunca expor ao cliente — usada só em src/lib/supabase/admin.ts (server-only).
SUPABASE_SERVICE_ROLE_KEY=
